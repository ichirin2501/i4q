#!/bin/bash

set -ex
IPADDR=$1
USERNAME=$USER

ssh -t isucon@$IPADDR "cd /home/isucon/webapp/perl && git pull && ./env.sh carton install && sudo ./script/logrotate.sh && sudo service supervisord reload && sudo service nginx restart"
